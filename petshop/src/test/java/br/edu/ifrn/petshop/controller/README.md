# controller (testes)

Testes da camada `controller`. Verificam o comportamento dos endpoints
(status HTTP, corpo da resposta, validações de entrada), normalmente usando
`MockMvc` ou `TestRestTemplate`, com a camada `service` mockada.
